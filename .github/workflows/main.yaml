name: Fork CI placeholder

on:
  workflow_dispatch:

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - name: Explain CI status
        run: |
          echo "Automatic fork CI is intentionally disabled for now."
          echo "The Android repository depends on a separate private core submodule."
          echo "Configure a cross-repository read credential before enabling build/test CI."
