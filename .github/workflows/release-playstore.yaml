name: Fork Play Store Release placeholder

on:
  workflow_dispatch:

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - name: Explain Play Store status
        run: |
          echo "Play Store publishing is disabled in this fork."
          echo "The upstream publishing workflow is intentionally not active."
