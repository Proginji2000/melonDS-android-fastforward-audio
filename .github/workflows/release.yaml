name: Fork GitHub Release placeholder

on:
  workflow_dispatch:

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - name: Explain release status
        run: |
          echo "Automatic release publishing is disabled in this private development fork."
          echo "Do not enable signed APK publishing until fork-specific signing, source-distribution and release checks are configured."
